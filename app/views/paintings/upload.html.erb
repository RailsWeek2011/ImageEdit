<%= form_for(:painting, :html => {:multipart => true, :method=>:put}) do |f|%>
	<p>
		<%= f.label :name %><br />
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.file_field :image %>
	</p>
	<p><%= f.submit "Image speichern" %></p>
<% end %>
